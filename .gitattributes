# Normalize to LF in the repository. Development is on Windows but the
# production target (Hostinger) is Linux, so the committed tree stays LF.
* text=auto eol=lf

# Binary assets — never touch these.
*.png binary
*.jpg binary
*.jpeg binary
*.webp binary
*.avif binary
*.ico binary
*.woff binary
*.woff2 binary
*.pdf binary
